<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p> <%= @task.completed %> </p>

<%= link_to "<Edit a new task", edit_path %>
  <br>
<%= link_to "<Delete a new task", destroy_path, method: :delete, data: { confirm: "Are you sure?"}  %>
  <br>
<%= link_to "Back to my tasks", tasks_path %>

