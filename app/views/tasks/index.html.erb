<h1>My tasks</h1>
<% @tasks.each do |task| %>
<h2><%= link_to task.title, task_path(task) %></h2>
  <p> <%= check_box("tag", tag.title, {completed: task.completed}) %> <%= task.details %>
  </p>
<% end %>




<%= link_to "Create a new task", new_path %>

